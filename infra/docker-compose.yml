version: "3.8"

services:
  corretor-imoveis-db:
    image: postgres:16
    container_name: corretor-db
    restart: unless-stopped
    ports:
      - "5437:5432"
    environment:
      - ../.env
    volumes:
      - /tmp/postgres-data:/var/lib/postgresql/data
